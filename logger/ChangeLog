2014-07-31  Jérôme Pasquier  <rainbru@free.fr>
	* src/tools/main_window.rb (feed_cbSources): feed source files combo.
	* src/tools/log_reader.rb (open_file): now creates an abstract object.

	* src/tools/log_reader.rb (parse_logfile_v1_psych): correctly
	flatten the message arrah of hashes.
	* src/tools/log_reader.rb (get_psych_node_content): now works
	for program details.
	* src/tools/log_reader.rb (open_file): now uses Psych.load_file.
	* src/tools/log_reader.rb (open_file): get 'logfile-version' value
	the Psych way.

2012-05-09  Jérôme Pasquier  <rainbru@free.fr>

	* src/examples/logger-test.cpp (main): uses new customizable values.

	* src/LoggerOutputYaml.cpp (LoggerOutputYaml): add a parameter to set
	logfile filename.

	* src/LoggerOutputFile.cpp (LoggerOutputFile): add a parameter to set
	logfile filename.

	* src/Logger.cpp (init): add some parameters to let user customize
	logger's application name and version and log filename.

2012-05-01  Jérôme Pasquier  <rainbru@free.fr>

	* rainbrurpg-logger.texinfo: now really rainbrurpg-logger related.
	Was an autoproject skeleton.

	* src/Logger.cpp (init): handle '?' shortcut for help and 'V' for
	version as command line options.

2012-05-01  Jérôme Pasquier  <rainbru@free.fr>

	* src/tools/log_reader.rb: fix a bug preventing main window to appear.

	* src/LoggerOutputYaml.cpp: many output functinos now use the
	escape macro.
	* src/LoggerOutputYaml.cpp (_e): created a simple shortcut macro to
	Rpg::LoggerOutputYaml::escape().

2012-04-30  Jérôme Pasquier  <rainbru@free.fr>

	* src/LoggerOutputYaml.cpp: uses ObjectInspector accessors.
	* src/LoggerOutputTty.cpp: uses ObjectInspector accessors.
	* src/ObjectInspector.hpp: make all members private, accessed through
	accessors.

	* src/LoggerOutputYaml.cpp: uses ObjectAttribute accessors.
	* src/LoggerOutputTty.cpp: uses ObjectAttribute accessors.
	* src/ObjectAttribute.hpp: make all members private, accessed through
	accessors.

	* logger.spec.in: filename now prefixed with 'rainbrurpg-' to fix
	conflict with bsdutils' logger binary.
	* logger.texinfo: filename now prefixed with 'rainbrurpg-' to fix
	conflict with bsdutils' logger binary.
	* logger.1: filename now prefixed with 'rainbrurpg-' to fix
	conflict with bsdutils' logger binary.

2012-03-20  Jérôme Pasquier  <rainbru@free.fr>

	* configure.in: some checks move to the m4/ directory.

	* src/Logger.cpp (init): makes help messages for program options
	translatable.

2012-03-14  Jérôme Pasquier  <rainbru@free.fr>

	* src/LoggerOutputTty.cpp (logHeader): now using gettext.

	* src/LoggerOutputTty.hpp: remove 'using namespace std'.
	* src/LoggerOutputYaml.hpp: remove 'using namespace std'.
	* src/LoggerOutputFile.hpp: remove 'using namespace std'.
	* src/LoggerOutput.hpp: remove 'using namespace std'.
	* src/ObjectAttribute.hpp: remove 'using namespace std'.

2012-03-10  Jérôme Pasquier  <rainbru@free.fr>

	* configure.in: search for Boost.Format with AC_CHECK_HEADERS.

2012-03-08  Jérôme Pasquier  <rainbru@free.fr>

	* Makefile.am: remove multiple definition of EXTRA_DIST and
	man_MANS.

	* configure.in: added invocation to AM_GNU_GETTEXT and
	AM_GNU_GETTEXT_VERSION.

2012-03-08  gettextize  <bug-gnu-gettext@gnu.org>

	* Makefile.am (SUBDIRS): Add po.
	(ACLOCAL_AMFLAGS): New variable.
	(EXTRA_DIST): Add config.rpath, m4/ChangeLog.
	* configure.in (AC_OUTPUT): Add po/Makefile.in.

2012-03-08  Jérôme Pasquier  <rainbru@free.fr>

	* src/LoggerDef.hpp: gettext macro implemented.

	* src/Logger.cpp (liblogger_initialize): implementation of the
	library initialization for gettext.

2012-02-20  Jérôme Pasquier  <rainbru@free.fr>

	* Makefile.am: Many changes due to removing generated files.

	* .: removed some autoproject generated files, including logger.cc,
	xmalloc.* and getopt*.

	* configure.in(AC_INIT): change parameter from logger.cc
	to src/Logger.cpp. It was an autoproject generated file.

2012-02-02  Jérôme Pasquier  <rainbru@free.fr>

	* src/tool/log_reader.rb: deactivated dialog's cancel button
	because of a segfault when exitting QApplication.
	* src/tool/log_reader.rb: HTML markup in dialog works again.

	* src/tool/log_reader.rb: Makes a dialog appears at startup and
	let the user choose to continue.


2012-02-02  Jérôme Pasquier  <rainbru@free.fr>

	* src/tools/Makefile.am (EXTRA_DIST): Added missing files MainWindow.ui,
	log_reader.rb and test.yaml to archive.
	* src/examples/Makefile.am (HEADERS): Added missing files Point.hpp
	to archive.
	* src/Makefile.am (h_sources): Added missing files Object.hpp
	to archive.

2012-01-25  Jérôme Pasquier  <rainbru@free.fr>

	* src/tool/Makefile.am: fix the rule builing MainWindow_ui.rb.

2012-01-19  Jérôme Pasquier  <rainbru@free.fr>

	* src/tool/log_reader.rb: print execution date and time in the UI.

	* src/LoggerOutput*.cpp (LogHeader): print execution date and time
	according to the output style.

	* src/Logger.cpp (init): feed the execution date and time fields.
	* src/LoggerOutput.hpp (LogHeader): added execution date and time 
	fields.

2012-01-12  Jérôme Pasquier  <rainbru@free.fr>

	* src/Logger.hpp (init): uses default values to set binary compilation
	date and time and not the library ones.

2012-01-11  Jérôme Pasquier  <rainbru@free.fr>

	* src/tool/log_reader.rb: print compil date and time in the UI.

	* src/LoggerOutput*.cpp (LogHeader): print compil date and time
	according to the output style.

	* src/Logger.cpp (init): feed the compil date and time fields.
	* src/LoggerOutput.hpp (LogHeader): added compil date and time fields.

2012-01-07  Jérôme Pasquier  <rainbru@free.fr>

	* rainbrurpg-logger.pc.in: pkg-config file generated and installed.

	* src/LoggerOutputTty.cpp  (startLog): change the Tty output
	colors to fix gray message issue

2011-12-09  Jérôme Pasquier  <rainbru@free.fr>

	* src/LoggerOutputYaml.cpp (escape): strings are in double quotes.
	* src/LoggerOutputYaml.cpp (logHeader): writes logfile-version.

	* src/tools/log_reader.rb: checks if passed files exists.
	* src/tools/log_reader.rb: opens file passed as argument.

2011-10-31  Jérôme Pasquier  <rainbru@free.fr>

	* Logger.hpp: Moved some functions to implementation.

2011-10-29  Jérôme Pasquier  <rainbru@free.fr>

	* configure.in: check for Boost.Program_options header

2011-07-16  Jérôme Pasquier  <rainbru@free.fr>

	* src/ObjectInspector: in its own files.
	* src/LoggerOutputTxt.hpp: in its own files.
	* src/LoggerOutputTty.hpp: renamed from LoggerOutputTxt.

2011-06-24  Jérôme Pasquier  <rainbru@free.fr>

	* src/tools/log_reader.rb: starts to parse hard-coded YAML.

2011-06-23  Jérôme Pasquier  <rainbru@free.fr>

	* src/LoggerOutputYaml: first tests.

	* src/tools/log_reader.rb: first runtime UI loading.

	* src/LoggerOutputTxt: in its own file.

	* logger: initial version.

