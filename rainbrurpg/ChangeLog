2009-04-03 Jerome Pasquier  <rainbru@free.fr>
	* common/objectpos.cpp : Uses constructor initializers
	
	* common/xmllanguage.cpp : Now computes complete percent.
	* config/language.xml : Added PoMessagesNumber and renamed 
	CompletePercent to TranslatedMessages.

2009-04-01 Jerome Pasquier  <rainbru@free.fr>
	* common/netpacketbase.cpp : Using gettext strings and a macro.
	* common/itemlistfile.cpp : Using gettext strings and a macro.
	* common/httpresponse.cpp : Using gettext strings and a macro.
	* common/hashfile.cpp : Using gettext strings.
	* common/globaluri.cpp : Using gettext strings.
	
	* common/Makefile.am : Added boostimport.h
	
	* common/nclude_boost.h : Renamed boostimport.h.

2009-03-31 Jerome Pasquier  <rainbru@free.fr>
	* common/ftpclient.cpp : Now uses gettext.
	* common/enetclient.cpp : Now uses gettext.
	* common/enetbase.cpp : Now uses gettext.
	* common/EasyBMP.cpp : Now uses gettext.
	
2009-03-31 Carine Chum <carine.chum@free.fr>
	* common/include_boost.h : Using this header
	instead a direct boost include fix a lot of compilation
	errors in the common library.

	* common/Makefile.am: Added version numbers for boost libraries
	name to be compliant with the dlls.
	
2009-03-30 Carine Chum <carine.chum@free.fr>
	* acinclude.m4 : Adding version to boost libraries names.
	
	* common/loadbmp.cpp : Now using namespace alias for 
	boost::filesystem.
	* common/globaluri.cpp : Now using namespace alias for 
	boost::filesystem.
	* common/curlgetfile.cpp : Now using namespace alias for 
	boost::filesystem.

2009-03-28 Jerome Pasquier  <rainbru@free.fr>
	* common/curlsubmit.cpp : now uses gettext.
	* common/curlservermodifclients.cpp : now uses gettext.
	* common/curlserverdelete.cpp : now uses gettext.
	* common/curlserveradd.cpp : now uses gettext.
	
	* common/Makefile.in : header/sources lists now sorted.

2009-03-27 Jerome Pasquier  <rainbru@free.fr>
	* common/curlposteddata.cpp : Now uses gettext.
	* common/curlgetfile.cpp : Now uses gettext.
	* common/curlgetfile.cpp : Prepare strings to be translated
	using sprintf instead of LOGCAT*.
	
	* ogreGui/bggui.cpp (injectRightMouseButtonPressed): Fix a bug
	in the right mouse button handling that avoid the drawing of
	the PopupMenu of the MultiColumnList.
	
2009-03-18  gettextize  <bug-gnu-gettext@gnu.org>

	* Makefile.am (SUBDIRS): Add po.
	(ACLOCAL_AMFLAGS): New variable.
	(EXTRA_DIST): Add config.rpath, m4/ChangeLog.
	* configure.in (AC_CONFIG_FILES): Add po/Makefile.in.

2009-03-18 Jerome Pasquier  <rainbru@free.fr>
	* common/attributemodifierlist.cpp : Now using gettext.
	* common/chartbase.cpp : Now using gettext.
	
2009-02-16 Jerome Pasquier  <rainbru@free.fr>
	* ogreGui/sobetagui.cpp (drawToolTip): Better fix for the
	tooltip drawing bug.

	* ogreGui/sobetagui.cpp (drawToolTip): Fix a bug when drawing tootip.

2009-02-13 Jerome Pasquier  <rainbru@free.fr>
	* ogreGui/dialog.cpp (draw): Now handle the geometryDirty flag to
	avoid a bug when moving a dialog.

	* ogreGui/scrollpane.h : resize() implementation.
	
2009-02-12 Jerome Pasquier  <rainbru@free.fr>
	* ogreGui/messagebox.h : function initWindow() removed.

	* client/guimanager.cpp (showMessageBox) : Uses the new MessageBox.
	* ogreGui/messagebox.h : Become a subclass of Dialog.
		
2009-02-11 Jerome Pasquier  <rainbru@free.fr>
	* ogreGui/bgwindow.h : Constructor now takes a const String& argument.
	* ogreGui/dialog.h : Constructor now takes a const String& argument.

2009-02-10 Jerome Pasquier  <rainbru@free.fr>
	* ogreGui/widget.h : Constructor now takes a const Vector4& argument.
	* ogreGui/bgwindow.h : Constructor now takes a const Vector4& argument.
	* ogreGui/dialog.h : Constructor now takes a const Vector4& argument.

	* ogreGui/dialog.cpp (draw) : Now call QuadRenderer::reset().

2009-02-05 Jerome Pasquier  <rainbru@free.fr>
	* ogreGui/bgwindow.cpp (resize) : Added support for NULL pointer
	for ResizeGrip and TitleBar.
	
	* ogreGui/dialog.cpp : Changing constructor from OWT_MOVE to
	OWT_RESIZE_AND_MOVE to test if the segfault is due to a spurious
	ResizeGrip pointer. It is not the solution.
	
2009-02-02 Jerome Pasquier  <rainbru@free.fr>
	* client/guimanager.cpp (showMessageBox) : Comment
	simpleDialog->show() for test purpose.
	
2009-02-01 Jerome Pasquier  <rainbru@free.fr>
	* ogreGui/scrollpane.cpp (draw): Added assertion to test
	NULL DrawingDevList pointer;

	* ogreGui/quadrenderer.cpp (begin): Retrieve a new RenderSystem
	pointer from OgreRoot if the one we have is spurious.

	* maincore/gameengine.cpp : Added some assertions.
	
	* ogreGui/quadrenderer.cpp : Added the mCreatorName member and its
	initializer in the constructor. It is a debugging member only.
	
2009-01-30 Jerome Pasquier  <rainbru@free.fr>
	* ogreGui/quadrenderer.cpp (debug) : Fixed a segfault that occurs
	when mDrawingDevList pointer was NULL.

	* ogreGui/quadrenderer.cpp (debug) : Now log the state.
	* ogreGui/quadrenderer.cpp (begin) : Now uses debug() if 
	ViewPort is NULL.


2009-01-30 Jerome Pasquier  <rainbru@free.fr>
	* ogreGui/quadrenderer.cpp (stateToString) : implemented.
	
	* ogreGui/quadrenderer.cpp (reser) : Set mState to QRS_RESET.
	* ogreGui/quadrenderer.cpp (end) : Set mState to QRS_END.
	* ogreGui/quadrenderer.cpp (begin) : Set mState to QRS_BEGIN.
	* ogreGui/quadrenderer.cpp (QuadRenderer) : Constructor set
	mState to QRS_UNSET.
	* ogreGui/quadrenderer.h : tQuadRendererState enumeration and
	mState class member added.
	
2009-01-29 Jerome Pasquier  <rainbru@free.fr>
	* client/gsmenubase.cpp (mousePressed) : Trying to remove
	the injectMouse() call to prevent the same segfault : works but
	another segfault occurs.
	* ogreGui/bgbui.cpp (handleWindowsEvents) : Added an assertion
	to try to prevent a segfault but failed.
	
2009-01-29 Carine Chum <carine.chum@free.fr>
	* acinclude.m4 : Changing some cross-compil stuff to prevent
	undefined reference when linking the executable.
	
2009-01-28 Jerome Pasquier  <rainbru@free.fr>
	* ogreGui/mousepointer.cpp (setState) : Setting the parameter a
	const reference to try to avoid segfault but failed.

2009-01-28 Carine Chum <carine.chum@free.fr>
	* acinclude.m4 : Added support for the $DLL_EXT variable to
	some cross-compilation aware macros.
	* configure.in : Change the $DLL_EXT variable value from an empty
	string to '.dll'.

2009-01-27 Jerome Pasquier  <rainbru@free.fr>
	* acinclude.m4 (RB_CHECK_LIBCURL) : Added support for
	cross-compilation to win32.
	
2009-01-16 Jerome Pasquier  <rainbru@free.fr>
	* common/curlfiletoxml.cpp (CurlFileToXml) : Added a constructor
	initializer for the doc pointer to avoid an instance creation with 
	non-NULL value.
	
2009-01-13 Jerome Pasquier  <rainbru@free.fr>
	* common/xmlaccountlist.cpp (refresh) : A std::cout call replaced 
	by a LOGCAT* one to avoid std out print when unit test.
	* common/xmlattrbcatlist.cpp (refresh) : A std::cout call replaced 
	by a LOGCAT* one to avoid std out print when unit test.
	* common/xmlattributelist.cpp (refresh) : A std::cout call replaced 
	by a LOGCAT* one to avoid std out print when unit test.
	* common/xmlbonusfilelist.cpp (refresh) : A std::cout call replaced 
	by a LOGCAT* one to avoid std out print when unit test.
	* common/xmlbonusfile.cpp (refresh) : A std::cout call replaced by a
	LOGCAT* one to avoid std out print when unit test.

2009-01-13 Jerome Pasquier  <rainbru@free.fr>
	* common/xmlpersolist.cpp (refresh) : A cout call replaced by a
	LOGCAT* one to avoid std out print when unit test.
	
2009-01-11 Jerome Pasquier  <rainbru@free.fr>
	* common/xmlserverlist.cpp (getServerList) : Now returns NULL if
	doc is NULL (an error occured).
	* common/xmlserverlist.cpp (xmlAccountList) : Add some initializers
	to avoid segfault due to non-NULL values.

	* tests/common/curlaccounttogglebacklisttest.h (testFinalValue) :
	Now test if the returned item is not NULL to avoid a segfault.
	* tests/common/curlaccounttogglebacklisttest.h (testInitialValue) :
	Now test if the returned item is not NULL to avoid a segfault.
	
	* common/xmlaccountlist.cpp (xmlAccountList) : Add some initializers
	to avoid segfault due to non-NULL values.
	
2009-01-08 Jerome Pasquier  <rainbru@free.fr>
	* editor/Makefile.am : fix an error due to MOC files, linked twice
	* server/Makefile.am : fix an error due to MOC files, linked twice
