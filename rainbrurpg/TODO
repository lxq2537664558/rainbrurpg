TODO

o [mouse] OgreGuiToolkit : GUI over Ogre3D based on QuadRenderer
- SEGFAULT when coming back from gsConnection to gsMainMenu
- MulticolumnList implementation
- gsServerList uses OgreGUI (with MulticolumnList)

* Unittest failed in production mode (no --enable-debug)
- HTTP response 401 "Unauthorized"

* cross-compilation to win32
- fix the config.h warnings (PACKAGE redefined...)

* Xident
- AddAccount dialog

* Maintenance
- The account is created only if the mail was correctly sent
- CurlAccountAdd::controlAfter tests if the account was correctly created

* Client 
- MessageBox can be closed with Enter/Return key
- gsUpdateDataFiles implementation
- client uses EnvironmentSetup in libRainbruRPG-core.so
  Waiting to be to the same rendering level with EnvironmentSetup
  (Fog and Skydome)
- Client's debug console uses the readline library

* Editor identification
   The identification is done by GPG keys
- The server let manage a list of editors
- The editor can contact a server to ask an authorisazion
- In the editor preferences we can choose a gpg key that will be use
   for all connexions

* GameServer
- The HelpViewer is called twice 
   when we click the OK button it appears again
- Running 2 servers in the same PC is forbiden
   A file running-server is created in the $HOME directory when the
   server is running. When we exit the server application, we delete
   this file. 
   ServerLock::lock()
   ServerLock::unlock()
   ServerLock::isLock()

- The database must be connected even if the server is not started
  To look for objects without connecting the server
- Update the client number in statusbar
- A dialog to export the program log (LogExportDialog)
  Chose the filename of the export
- Modele Conceptuel de Donnees pour la base de donnees
  Dans le repertoire rpg/sql se trouve le script de creation de la
  base et de ses tables. Il s'agit pour l'instant de faire une bdd
  minimale qui acceptera la modification des positions et orientation
  des objets par connection au serveur.

* Editor
- PosorDialog initialization with the ObjectPos values
  PosorDialog::setPosName must know the ObjectMesh name
- OgreView cleanup
  Give up unused functions ans global variables 
- SelectionCycle teste si le nom d'Entity retourne contient Outline
	- Cela evitera un Segfault
  [ogreview.cpp:1178]           -- Selected object name : Arbre1_1Outline
  [selectionmanager.cpp:107]    II Deleting last selection highlight
  [selectionmanager.cpp:121]    II Creating selection highlight
  Erreur de segmentation

- SelectionManager gere la selection multiple
- Un overlay d'aide lors de la selection d'un mesh
- Destructeur de MeshViewer 
  Permet la possibilite d'afficher 2 MeshViewer l'un apres l'autre
  pour l'instant, cela cause un segfault a la deuxieme ouverture
- Environmentsetup utilise le skydome et le fog

* FlooderTest
- Test the implementation with the packet size (how the performance
   decrease) (ftSize)
- Constant time flooding (2ms or 200ms : packet lost?) (ftTime)

* Fil rouge
- test Valgrind
  Pour eviter les fuites memoires et autres.
- manual pages : RainbruRPG.6 RainbruRPG-editor.6 RainbruRPG-server.6
- texinfo manual : RainbruRPG.texinfo

* Divers
- modifpersoattrb.php ne teste pas si les champs sont vides, 
  Resultat les champs sont mis a vide.
- CurlSubmitForm::computePostedData est-il utilise?
- Un fichier adminmsg.xml contenant :
	- Un message de l'admin au joueur
	- Les messages des joueurs a l'admin
	- Si l'admin rend temporairement impossible la connection, dans combien
	  de temps le serveur sera de nouveau online. Rafraichissement de la
	  durée à l'aide de xident.
- Se renseigner sur Audiere



* *******************************************************
* ***************** Changelog ***************************
* *******************************************************

* v 0.0.5-172

* v 0.0.5-171
- GlobalURI unit test imlemented
- Homepage news
[Title]ScrollPane implementation in progress
[Content]The ScrollPane widget is about to be implemented even if some 
modifications are needed in the event handling os ScrollBars and
Window's TitleBar and ResizeGrip. The first tests prove that the use 
of scissor rectangle to chop off the primitives drawn by QuadRenderer 
to the ScrollPane content was a good idea.[br]
[br]
At the same time, I carry on the implementation of unit tests, actually 
on the common/ library (FtpClient and GlobalURI classes).
- Unit test works in Qt3 version (not only test version)
- common/ftpclient unit test implementation
- Add some functions to common/ftpclient for unit tests implementation
- ScrollBars drawn by ScrollPane
- Documentation update (many undocumented members)
- CurlInterface::setWrittenData() function removed (unused)
- ResizeGrip event handled by Window
   Container::handleButtonEvent() imlementation
   Window uses mScrollPane::handleButtonEvent(mResizeGrip);
- TitleBar, ResizeGrip drawn by Window
- Nothing is drawn if ResizeGrip not added to the ScrollPane
   Window::setTransparency did not set alpha for ResizeGrip and titlebar
- QuadRenderer::debug() implementation
   Used to find a bug in ResizeGrip
- QuadRenderer drawingDev implementation
- Connection ScrollPane's scrollbar signals to slots
- Testing Scrollbar when policy is Always/Never
- Recompute scrollbar visibility when widget is added
- Move and Resize scrollbar when Window is resized
  Using ScrollPane::setWidget and Height
- Widget set* and move, resize makes geometry dirty
- Handle ScrollBars event only if visible

