				Hey emacs, this is a -*-todoo-*- file

OVERALL TODO

* Enable travis-ci build on these projects
- before_install in a CI/before_install.sh
   
* TODO: release-all.sh
- Make a complete archive containing all code from actual sub-projects
   with a README file containing building order/instructions.
- We have to call autoreconf&&./configure&&make dist-bzip2 for each
   subdir
-  call `make dist-bzip2` in each subdirs but OLD/ and create a
   'rainbrurpg-all-src' global archive using the stats.pl version
   number. It must first test if each directory doesn't contain
   more than one tarball.

* SPARETIME : switch to NewProjectLayout
- client uses new logger

* v0.0.7
- Website revamping (skin, blog, wiki, API documentation, maybe 
  texinfo manuals...)

* Others versions...
- DIS review (http://en.wikipedia.org/wiki/Distributed_Interactive_Simulation)
- Metaserver (foswiki plugin, C++ API)
- Server register on metaserver (+ ruby extension + ruleset)
- ongame chat (needs network/ASN.1 support ?)
- cli chat (need metaserver modification)

=== ChangeLog ===
   (10 items by revision)
   SLOC is the result of `sloccount src/`
   FT is the result of `sh FT.sh`
   Add git tag r-1 when switching
   
* v0.0.5-254 (23 Nov. 2016 - ???) SLOC ?,??? FT ???:?? UT ?
- (5) GUI font is not scaling anymore
- TempMessage now uses DejaVuSans-10-NoScale
- Disable TempMessage to fix a segfault
- Auto-save configuration on fullscreen/resolution changes
- Print fullscreen status after reconfigure
