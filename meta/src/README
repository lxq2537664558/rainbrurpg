This is the root directory of the RainbruRPG's metaserver source tree.

The =data= and =lib= directory go in the same directpry hierachy as in
_foswiki_. These directories contain implementation and documentation for
the foswiki metaserver plugin.